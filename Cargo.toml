# One workspace so the engine and the Ruby extension share a target
# directory: both link the same MLX archives, and a second copy of that
# build is not worth the disk.
[workspace]
members = ["engine", "ext/torobi"]
resolver = "2"
